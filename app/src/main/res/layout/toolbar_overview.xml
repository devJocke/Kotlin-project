<?xml version="1.0" encoding="utf-8"?>

<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_marginStart="12dp"
    android:orientation="horizontal">

    <TextView
        style="@style/TextAppearance.Widget.AppCompat.Toolbar.Title"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:gravity="start|center_vertical"
        android:text="@string/title_over_view"
        android:textSize="18sp"/>

    <!-- Apply the manually set inset of the toolbar, and then apply the imageviews deviceWidthidth + the inset -->
    <ImageView
        android:id="@+id/club_crest_image_view"
        style="@style/TextAppearance.Widget.AppCompat.Toolbar.Title"
        android:layout_width="52dp"
        android:layout_height="52dp"
        android:layout_gravity="center"
        android:layout_marginEnd="16dp"
        android:contentDescription="@string/content_descript_club_crest"/>

    <View
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"/>

</LinearLayout>